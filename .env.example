# 复制为 .env 后填写（docker compose 会注入容器环境，供 config.yaml 中 ${VAR} 使用）
OPENAI_API_KEY=
WECHAT_APP_ID=
WECHAT_APP_SECRET=
# 仅在使用 docker-compose.mysql.yml 时需要（与 config 里 root 密码一致）
MYSQL_ROOT_PASSWORD=onepaper
APP_PORT=8080
MYSQL_PORT=3306
